import CatalogueManager from "@/components/catalogue/CatalogueManager";

export default function ServicesManagement() {
  return <CatalogueManager portalType="staff" pageTitle="Manage Catalogue" />;
}
